# NDTV News Headline Scraper

This Python script scrapes the latest news headlines using `requests` and `BeautifulSoup`, and saves them to a text file.

## 📦 Requirements

- Python 3.x
- requests
- beautifulsoup4

Install dependencies:

```bash
pip install requests beautifulsoup4
